## 📌 4号（前端开发 - 游客交互端）进度同步

### ✅ 已完成

- 游客端 H5 页面开发完成
- 文件位置：`frontend/public/tourist.html`
- 适配移动端，界面简洁可用
- 已对接后端接口，使用 `/api/v1/...` 格式，符合团队规范

![游客端界面](../images/image5.png)
### 🔗 访问方式

```（需先启动前端 `npm run dev` 和后端 `python main.py`）
http://localhost:5173/tourist.html
```



### 📡 已对接的接口

| 接口 | 用途 | 状态 |
|------|------|------|
| `GET /api/v1/config` | 获取数字人配置（名称、音色） | ✅ 5号已提供 |
| `POST /api/v1/chat/ask` | 发送提问，获取 AI 回答 | ✅ 5号已提供（目前返回假数据）|

### 🎯 页面功能

| 功能 | 说明 |
|------|------|
| 文字提问 | 输入框输入 → 点发送 / 按回车 |
| 语音输入 | 点麦克风 → 允许权限 → 说话自动转文字 |
| 对话展示 | 用户消息蓝色，AI 消息白色 |

---

### 🤝 需要各位配合的事项

**致 2号（AI大模型 + 知识库）**

- 游客端已对接 `/api/v1/chat/ask`，目前返回的是假数据
- 你接入大模型后，保持返回格式不变即可，前端无需修改
- 格式：`{ code: 200, data: { answer: "回答内容" } }`
- 完成后告诉我一声，我测试一下即可

**致 3号（数字人渲染/TTS）**

- 数字人展示区已预留（`class="avatar-wrapper"` 的圆形区域），目前是静态图标
- 数字人配置接口：`GET /api/v1/config`（5号已提供，返回 avatar_style、voice_type 等）
- 需要我配合什么？（比如替换容器、不从对话记录拿而需要传递 AI 回答文本、用户选择不同音色等），告诉我一声
- 目前ai语音回答只是转文字，后续如果需要播放 AI 回答的语音，也请告诉我接口和数据格式，我这边配合实现

**致 5号（后端架构）**

- 游客端没有集成到 Vue 管理后台，是独立静态页面 ✅
- 所有请求使用相对路径 `/api/v1/...` ✅
- 感谢你提供的接口，一切顺利 🙏

---

### 📂 文件结构

```
scenic-digital-human/
├── frontend/
│   ├── public/
│   │   └── tourist.html    ← 游客端在这里
│   └── ...
├── backend/
│   └── main.py
└── ...
```

---

有问题随时qq找我，看到就回 💪
